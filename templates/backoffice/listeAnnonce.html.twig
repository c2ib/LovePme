{% extends 'base.html.twig' %}

{% block title %}Hello newCompanyController!
{% endblock %}

{% block body %}
<table class="table caption-top">
  <caption>List of annonces</caption>
  <thead>
    <tr>
      <th scope="col">Client</th>
      <th scope="col">Type</th>
      <th scope="col">societe</th>
      <th scope="col">Quantité</th>
      <th scope="col">Prix unitaire</th>
    </tr>
  </thead>
  {% for annonce in annonces %}
  <tbody >
    <tr>
      <th scope="row"><a href="{{path('Details_Annonces', {'id': annonce.idUser.id})}}" class="btn btn-primary">{{ annonce.idUser.firstName }} {{ annonce.idUser.lastName }}</a>
      <td>{{ annonce.type}}</td>
      <td>{{ annonce.quantite}} action de {{ annonce.idSociete.name }}</td>
      <td>{{ annonce.prixUnitaire }}</td>
      <td>{{ annonce.quantite}}</td>
    </tr>
  </tbody>
  {% endfor %}
</table>
{% endblock %}

