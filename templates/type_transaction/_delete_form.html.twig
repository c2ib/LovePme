<form method="post" action="{{ path('type_transaction_delete', {'id': type_transaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_transaction.id) }}">
    <button class="btn">Delete</button>
</form>
