{% extends 'base.html.twig' %}

{% block title %}TypeTransaction{% endblock %}

{% block body %}
    <h1>TypeTransaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_transaction.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ type_transaction.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_transaction_index') }}">back to list</a>

    <a href="{{ path('type_transaction_edit', {'id': type_transaction.id}) }}">edit</a>

    {{ include('type_transaction/_delete_form.html.twig') }}
{% endblock %}
