{% extends 'base.html.twig' %}

{% block title %}Edit Action{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                <h1>Edit Action</h1>

                {{ include('action/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('action_index') }}">back to list</a>

                {{ include('action/_delete_form.html.twig') }}

            </div>
            <div class="col-md-3"></div>
        </div>
    </div>
    {% endblock %}
