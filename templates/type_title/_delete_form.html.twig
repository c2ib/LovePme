<form method="post" action="{{ path('type_title_delete', {'id': type_title.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_title.id) }}">
    <button class="btn">Delete</button>
</form>
