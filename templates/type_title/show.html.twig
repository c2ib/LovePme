{% extends 'base.html.twig' %}

{% block title %}TypeTitle{% endblock %}

{% block body %}
    <h1>TypeTitle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_title.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_title.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_title_index') }}">back to list</a>

    <a href="{{ path('type_title_edit', {'id': type_title.id}) }}">edit</a>

    {{ include('type_title/_delete_form.html.twig') }}
{% endblock %}
