{% extends 'base.html.twig' %}

{% block title %}FormeLegale{% endblock %}

{% block body %}
    <h1>FormeLegale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forme_legale.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ forme_legale.name }}</td>
            </tr>
            <tr>
                <th>Shortname</th>
                <td>{{ forme_legale.shortname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('forme_legale_index') }}">back to list</a>

    <a href="{{ path('forme_legale_edit', {'id': forme_legale.id}) }}">edit</a>

    {{ include('forme_legale/_delete_form.html.twig') }}
{% endblock %}
